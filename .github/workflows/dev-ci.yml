name: Dev Branch CI

on:
  pull_request:
    branches: [dev]
  push:
    branches: [dev]

jobs:
  dev-checks:
    uses: blurgsai/ci-workflows-software-team/.github/workflows/dev-ci.yml@main
    secrets: inherit
