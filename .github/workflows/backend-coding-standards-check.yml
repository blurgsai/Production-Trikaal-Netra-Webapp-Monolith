name: Backend Coding Standards Check

on:
  pull_request:
    branches: [main, develop]
    paths:
      - 'backend/**'
      - '.github/workflows/backend-coding-standards-check.yml'
      - '.github/scripts/check-backend-naming-conventions.sh'
  push:
    branches: [main, develop]
    paths:
      - 'backend/**'
      - '.github/workflows/backend-coding-standards-check.yml'
      - '.github/scripts/check-backend-naming-conventions.sh'

jobs:
  backend-coding-standards:
    name: Backend Naming & Coding Standards Validation
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # ─── Check 1: Naming Conventions ───
      - name: Check naming conventions (files, functions, classes, variables)
        run: |
          chmod +x ../.github/scripts/check-backend-naming-conventions.sh
          ../.github/scripts/check-backend-naming-conventions.sh src

      # ─── Check 2: Ruff Linter ───
      - name: Run ruff linter
        run: |
          if [ -d src/ ] && [ -n "$(find src/ -name '*.py' -not -name '__pycache__' 2>/dev/null)" ]; then
            ruff check src/
          else
            echo "SKIP: No Python source files in src/ -- skipping ruff"
          fi
